spring:
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB
  application:
    name: backdend

gcp:
  credentials:
    path: src\main\java\com\idn\backend\Credentials\googleCredentils.json
  bucket:
    name: blog-media-bucket
